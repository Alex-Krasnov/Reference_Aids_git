@model IEnumerable<Reference_Aids.Models.ListQualitySerum>
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

@{
    Layout = "_LayoutList";
}

<div class="container">
    <form class="needs-validation" novalidate method="post" >

        <div class="row g-0">

            <div class="col-sm-1">
                <label for="firstName" class="form-label"><font style="vertical-align: inherit;">Id</font></label>
            </div>

            <div class="col-sm-3">
                <label for="lastName" class="form-label"><font style="vertical-align: inherit;">Name</font></label>
            </div>

        </div>

    </form>

    @foreach (var item in Model) {
        <form class="needs-validation" novalidate asp-controller="ListQualitySerums" asp-action="Update" method="post"  asp-route-id="@item.QualitySerumId">
            <div class="row g-0" >

                <div class="col-sm-1">
                    <input type="text" class="form-control" id="QualitySerumId" name='QualitySerumId' value="@item.QualitySerumId">
                </div>

                <div class="col-sm-3">
                    <input type="text" class="form-control" id="QualitySerumName" name='QualitySerumName' value="@item.QualitySerumName">
                </div>

                <div class="col-sm-1"></div>

                <div class="col-sm-2 d-grid gap-2 d-md-block">
                    <a class="btn btn-outline-danger btn-sm" asp-controller="ListQualitySerums" asp-action="Delete" asp-route-id="@item.QualitySerumId">Удалить</a>
                    <input type="submit" class="btn btn-outline-success btn-sm" value="Редактировать" />
                </div>

            </div>
        </form>
    }
    
    <form class="needs-validation" novalidate asp-controller="ListQualitySerums" asp-action="Create" method="post">
        <div class="row g-0">
            <div class="col-sm-1">
                <input type="text" class="form-control" id="QualitySerumIdNew" name="QualitySerumId" value="">
            </div>
            <div class="col-sm-3">
                <input type="text" class="form-control" id="QualitySerumNameNew" name="QualitySerumName" value="">
            </div>
            <div class="col-sm-1"></div>
            <div class="col-sm-2 d-grid gap-2 d-md-block">
              <button type="submit" class="btn btn-outline-success btn-sm">добавить</button>
            </div>
        </div>

    </form>
</div>
