@model IEnumerable<Reference_Aids.Models.ListSendLab>
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

@{
    Layout = "_LayoutList";
}

<div class="container">
    <form class="needs-validation" novalidate method="post" >

        <div class="row g-0">

            <div class="col-sm-1">
                <label for="firstName" class="form-label"><font style="vertical-align: inherit;">Id</font></label>
            </div>

            <div class="col-sm-3">
                <label for="lastName" class="form-label"><font style="vertical-align: inherit;">Name</font></label>
            </div>

        </div>

    </form>

    @foreach (var item in Model) {
        <form class="needs-validation" novalidate asp-controller="ListSendLabs" asp-action="Update" method="post"  asp-route-id="@item.SendLabId">
            <div class="row g-0" >

                <div class="col-sm-1">
                    <input type="text" class="form-control" id="SendLabId" name='SendLabId' value="@item.SendLabId">
                </div>

                <div class="col-sm-3">
                    <input type="text" class="form-control" id="SendLabName" name='SendLabName' value="@item.SendLabName">
                </div>

                <div class="col-sm-1"></div>

                <div class="col-sm-2 d-grid gap-2 d-md-block">
                    <a class="btn btn-outline-danger btn-sm" asp-controller="ListSendLabs" asp-action="Delete" asp-route-id="@item.SendLabId">Удалить</a>
                    <input type="submit" class="btn btn-outline-success btn-sm" value="Редактировать" />
                </div>

            </div>
        </form>
    }
    
    <form class="needs-validation" novalidate asp-controller="ListSendLabs" asp-action="Create" method="post">
        <div class="row g-0">
            <div class="col-sm-1">
                <input type="text" class="form-control" id="SendLabIdNew" name="SendLabId" value="">
            </div>
            <div class="col-sm-3">
                <input type="text" class="form-control" id="SendLabNameNew" name="SendLabName" value="">
            </div>
            <div class="col-sm-1"></div>
            <div class="col-sm-2 d-grid gap-2 d-md-block">
              <button type="submit" class="btn btn-outline-success btn-sm">добавить</button>
            </div>
        </div>

    </form>
</div>
